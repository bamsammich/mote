//! The `mote` command-line surface.
//!
//! The `mote plugin …`, `mote secrets link`, etc. CLI. Thin; delegates to
//! `mote-pluginmgr`/`mote-secrets`. Can run without launching the engine (for
//! `add`/`diff`/`gc`/`sync`).
//!
//! This crate is a stub awaiting the `3.3` implementation wave.
