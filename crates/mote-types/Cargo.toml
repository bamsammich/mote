[package]
name = "mote-types"
description = "Shared vocabulary for Mote — the zero-domain-logic newtypes used everywhere: PluginName, Origin, Glob (permission-pattern matching with negation), SchemaVersion, Checksum (BLAKE3), and the identity/workspace/tab id types."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
blake3 = { workspace = true }
thiserror = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
