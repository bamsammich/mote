[package]
name = "mote-dispatch"
description = "Differentiated hook dispatch — filter chains, broadcasts, keybind handlers, and the collector pattern, with priority ordering, capability invocation under the fulfiller's permissions, and three-errors-in-24h auto-disable."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
mote-audit = { path = "../mote-audit" }
mote-lua = { path = "../mote-lua" }
mote-types = { path = "../mote-types" }
thiserror.workspace = true
